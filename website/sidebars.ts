import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// Explicit rather than autogenerated. The order is an argument: the numbers come
// first, then how they were produced, then what they do not cover — and a reader
// who stops after the first page should still have met the caveat.
const sidebars: SidebarsConfig = {
  docs: [
    'index',
    'workload',
    {
      type: 'category',
      label: 'The fairness contract',
      link: {type: 'doc', id: 'contract/rules'},
      items: ['contract/envelope', 'contract/measurement', 'contract/comparability'],
    },
    'environments',
    'limitations',
    'roadmap',
    'reproduce',
  ],
};

export default sidebars;
